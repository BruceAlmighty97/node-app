# node-app
This application has a node app api and an angular client

### To run the api:
* navigate to /api
* run "npm install"
* run "npm start"

### To run the angular app:
* navigate to /client
* run "npm install"
* run "npm start"
